{
    "Build": {
        "app": "gcc -c str.c",
        "unixlib": [
            "ar rcs str.a *.o",
            "mv str.a /usr/local/lib/libstr.a"
        ]
    },
    "Clean": {
        "rm -r {DIR}"
    }
}